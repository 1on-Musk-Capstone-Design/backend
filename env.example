# OpenAI API 설정
# OpenAI 웹사이트(https://platform.openai.com/api-keys)에서 API 키를 발급받아 입력하세요
OPENAI_API_KEY=sk-your-openai-api-key-here

# 데이터베이스 설정 (선택사항 - application.yml에서도 설정 가능)
DB_URL=jdbc:postgresql://localhost:5432/capstone_db
DB_USERNAME=postgres
DB_PASSWORD=your-db-password

# Socket.IO 설정 (선택사항)
SOCKETIO_HOST=localhost
SOCKETIO_PORT=9092

# 애플리케이션 설정
SPRING_PROFILES_ACTIVE=dev

# 사용 방법:
# 1. 이 파일을 .env로 복사하세요: cp env.example .env
# 2. .env 파일에서 실제 값들을 입력하세요
# 3. .env 파일은 절대 Git에 커밋하지 마세요 (보안상 위험)
